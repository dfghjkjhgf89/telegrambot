# This file makes Python treat the `db` directory as a package.

# You can optionally expose certain functions or objects from this package here,
# for example, by importing them from your query modules.
# from .queries import get_user, add_user # etc.

# Or, if you have a central database connection setup in a module like `connection.py` within `db`:
# from .connection import get_db_connection # Example

# For now, it can remain empty if direct imports from `db.queries` are preferred in other modules.
